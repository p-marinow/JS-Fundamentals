function numberPowering(number, power){
    number = Number(number);
    power = Number(power);
    let result = Math.pow(number, power);
    console.log(result);
}

numberPowering(3, 4)


//100 pts